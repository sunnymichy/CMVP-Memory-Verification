# Core ML / data (classifier reproduction -- any OS)
numpy
pandas
scikit-learn
scipy
xgboost
lightgbm
catboost
shap
matplotlib

# Structure verifier + collection (field study / data collection)
cryptography        # DER private-key validation, AES, Curve25519/Ed25519
pynacl              # libsodium / PyNaCl target library
pycryptodome        # PyCryptodome target library
pyaes               # pure-Python AES target library
ecdsa               # PurePy (unseen) library
rsa                 # PurePy (unseen) library

# Notes:
#  - Field study and perturbation_bench.py are Windows-only (ctypes psapi/ntdll/kernel32).
#  - Windows CNG (bcrypt.dll) is a system library, no pip package.
